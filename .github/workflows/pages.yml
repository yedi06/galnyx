# Publica el sitio en GitHub Pages desde galnyx-canvas/, que es la carpeta que
# genera `node build.mjs`. No hay paso de compilación en CI a propósito: lo que
# se publica es exactamente lo que se revisó en local, sin sorpresas entre lo
# aprobado y lo que sale en vivo.
name: Publicar en GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  desplegar:
    environment:
      name: github-pages
      url: ${{ steps.despliegue.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: galnyx-canvas
      - id: despliegue
        uses: actions/deploy-pages@v4
